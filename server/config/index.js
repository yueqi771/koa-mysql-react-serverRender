const config = {
    port: 3000,
    database: {
        DATABASE: 'yueqisql',
        USERNAME: 'root',
        PASSWORD: '',
        PORT: '3306',
        HOST: 'localhost'
    }
}

module.exports = config